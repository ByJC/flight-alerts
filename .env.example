# Get both from console.cloud.google.com → APIs & Services → Credentials → your Desktop OAuth client
# The "client secret" is required even for Desktop apps using PKCE — Google treats it as a public secret for installed apps.
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-secret
